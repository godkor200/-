let timeToGoHome = function (speed, distance) {
  let time = distance / speed;
  console.log(time);
};

function timeToGoHome(speed, distance) {
  let time = distance / speed;
  console.log(time);
}

let timeToGoHome = (speed, distance) => {
  let time = distance / speed;
  console.log(time);
};
